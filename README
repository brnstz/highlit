Source code for the website http://highl.it (defunct)

database schema for SQLite 

CREATE TABLE images(
    id INTEGER PRIMARY KEY,
    height INTEGER,
    width  INTEGER,
    top    INTEGER,
    left   INTEGER,
    ext    TEXT,
    auth   TEXT
);

